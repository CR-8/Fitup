{
    "version": 2,
    "buildCommand": "npm run build",
    "builds": [
        {
            "src": "api/index.js",
            "use": "@vercel/node",
            "config": {
                "includeFiles": ["dist/**", "public/**", "src/**", "config/**", "package.json", "node_modules/@strapi/**"]
            }
        }
    ],
    "routes": [{ "src": "/(.*)", "dest": "api/index.js" }]
}
